# Task

Task List (As Discussed)

Basic Setup
Set up a simple backend using TypeScript, Node.js, and Fastify, and create a basic “Hello World” API to demonstrate understanding of the setup and syntax.

Elasticsearch Integration
Install and configure Elasticsearch locally, connect it with the backend, and create endpoints to send and retrieve data in JSON format.

Mini Project – Todo API (CRUD)
Build a simple Todo API using TypeScript, Node.js, Fastify, and Elasticsearch, supporting basic CRUD operations (Create, Read, Update, Delete) with proper REST endpoints.